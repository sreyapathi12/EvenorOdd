# Even or Odd Checker

A simple Python program that checks whether a given number is even or odd.

## How It Works

The program uses the modulus (%) operator.

- If the remainder is 0, the number is Even.
- Otherwise, the number is Odd.

## Technologies Used

- Python
